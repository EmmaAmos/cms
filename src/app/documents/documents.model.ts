export class Contact {
    public id: string;
    public name: string;
    public description: string;
    public url: string;
    public children: string;
 
    constructor(id: string, name: string, name: string, name: string, name: string, ) {
        this.id = id;
        this.name = name;

    }
}